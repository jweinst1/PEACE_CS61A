"""Note: These problems deal with mathematical methods for integers, and how to manipulate them."""

def index_int(num, i):
    #Define a function, index_int, where you input an integer, and a selection, i, and return the ith index
    #of the integer. Assume the integer is 0 indexed, where the first element has the index of 0.
    #>>> index_int(566, 1)
    #6
    pass

def pop_int(num):
    #Define a function, pop_int, where you remove the last digit of an integer. Meaning 544 becomes 54. If the integer is
    #a single digit, raise a ValueError.
    #>>> pop_int(56)
    #5
    #>>> pop_int(55555)
    #5555
    pass
